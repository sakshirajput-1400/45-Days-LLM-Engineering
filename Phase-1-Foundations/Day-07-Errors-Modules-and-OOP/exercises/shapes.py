"""
Exercise 3 — Shapes & Inheritance (STUDENT STUB).

A base Shape with Rectangle and Circle subclasses. Practise super() + overriding.

Run:
    python shapes.py
"""

import math

# TODO: class Shape:
#   __init__(self, name): self.name = name
#   area(self): return 0           # placeholder the children override

# TODO: class Rectangle(Shape):
#   __init__(self, width, height): super().__init__("Rectangle"); store w/h
#   area(self): return width * height

# TODO: class Circle(Shape):
#   __init__(self, radius): super().__init__("Circle"); store radius
#   area(self): return math.pi * radius ** 2

# TODO: put several shapes in a list, loop, print f"{name}: area = {area:.2f}"
